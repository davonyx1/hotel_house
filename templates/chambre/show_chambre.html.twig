{% extends "base.html.twig" %}

{% block title %}Toutes les chambres{% endblock %}

{% block body %}
    <h1 class="text-center">Les différentes chambres</h1>

<div class="row">
    <div class="col-10 mx-auto my-4">
        <h2 class="text-center"> Toutes les Chambres</h2>

        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>Titre</th>
                <th>Description courte</th>
                <th>Description longue</th>
                <th>Photo</th>
                <th>Prix</th>
                <th>Date enregistrement</th>
                <th>Actions</th>
                        
            </tr>
            </thead>
                <tbody>
                {% for chambre in chambres %}
                    
                
                    <tr>
                            <td>{{ chambre.id }}</td>
                            <td>{{ chambre.id }}</td>
                            <td>{{ chambre.id }}</td>
                            <td>{{ chambre.id }}</td>
                            <td>
                            <img src="#" alt="photo de chambre" width="80" height="58" class="img-thumbnail">
                            </td>
                            <td>{{ chambre.id }}</td>
                            <td>{{ chambre.id }}</td>
                            <td>
                                <a href="#" title="Modifier Chambre" class="text-center align-middle text-warning"><i></i></a>
                                <a href="#" title="Modifier Chambre" class="text-center align-middle text-warning"><i></i></a>
                            
                            </td>
                    
                    
                    
                    </tr>
                
                {% endfor %}
                
                </tbody>

        </table>
    
    
    
    
    </div>
</div>










{% endblock %}